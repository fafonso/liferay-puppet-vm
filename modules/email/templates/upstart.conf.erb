#!upstart

description "Mailcatcher"
start on runlevel [23]
stop on shutdown
<% bin_path = (`echo "$(gem env)"|grep DIREC|grep EXEC| awk '{print $4}'`).strip %>

exec sudo -u vagrant <%= bin_path %>/mailcatcher --http-ip 0.0.0.0 --http-port <%=@mail_http_port%> --smtp-por <%=@mail_server_port%> -f

respawn
